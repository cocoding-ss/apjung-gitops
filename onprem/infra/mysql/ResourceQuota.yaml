apiVersion: v1
kind: ResourceQuota
metadata:
  name: mysql-resource
  namespace: mysql
spec:
  hard:
    requests.cpu: "250m"
    requests.memory: "256Mi"
    limits.cpu: "250m"
    limits.memory: "256Mi"
