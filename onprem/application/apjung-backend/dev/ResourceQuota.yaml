apiVersion: v1
kind: ResourceQuota
metadata:
  name: dev-resource
  namespace: dev
spec:
  hard:
    requests.cpu: "250m"
    requests.memory: "256Mi"
    limits.cpu: "250m"
    limits.memory: "256Mi"
