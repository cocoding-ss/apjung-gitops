apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: apjung-backend-ingress
  namespace: dev
  annotations:
    kubernetes.io/ingress.class: "nginx"
    nginx.ingress.kubernetes.io/rewrite-target: /
    nginx.ingress.kubernetes.io/proxy-body-size: 1g

spec:
  tls:
  - hosts:
      - api.apjung.xyz
    secretName: tls-secret-dev
  rules:
  - host: api.apjung.xyz
    http:
      paths:
      - path: /
        backend:
          serviceName: apjung-backend-svc
          servicePort: 8080
